<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="container.list">
      <field name="name">container list</field>
      <field name="model">container.container</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="container.action_window">
      <field name="name">container window</field>
      <field name="res_model">container.container</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="container.action_server">
      <field name="name">container server</field>
      <field name="model_id" ref="model_container_container"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="container" id="container.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="container.menu_1" parent="container.menu_root"/>
    <menuitem name="Menu 2" id="container.menu_2" parent="container.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="container.menu_1_list" parent="container.menu_1"
              action="container.action_window"/>
    <menuitem name="Server to list" id="container" parent="container.menu_2"
              action="container.action_server"/>
    -->
  </data>
</odoo>